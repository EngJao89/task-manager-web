import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // reactCompiler: true, // Temporariamente desabilitado para evitar problemas de build
};

export default nextConfig;
